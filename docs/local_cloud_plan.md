# 本地 + 云端双端规划（简版）

## 本地
- 用 Git 管理目录结构，确保离线可用。
- 依赖 `docs/index.md` 作为离线索引入口。
- 使用 `rg` 搜索模板与笔记。

## 云端
- Git 远端托管，建议开启 Issues 用于模板需求收集。
- 后续可接入静态站点生成（可选）。

## 同步策略
- 本地提交后推送云端。
- 重要模板更新打 tag，保留回滚能力。
