# 小程序方案：作业本姓名拍照识别与作业统计

## 目标与范围
- 面向老师日常收作业场景，**通过小程序拍照/导入图片识别作业本封面姓名**，自动统计每次作业的交付情况。
- 重点解决：**快速录入名单、批量识别、异常确认、导出统计表**。
- 交付形式：**微信小程序 + 轻量后端服务 + 数据库**。

## 业务流程（端到端）
1. **班级/名单录入**：老师在管理端或小程序内导入 Excel/CSV，形成班级学生库。
2. **创建作业任务**：每次作业生成任务（科目、日期、备注）。
3. **拍照/上传**：老师拍照或导入多张作业本封面。
4. **OCR 识别姓名**：识别文本 → 提取候选姓名。
5. **名单匹配**：精确匹配 + 模糊匹配（同音/错别字/少字）。
6. **人工确认**：低置信度或多候选时，提示老师确认。
7. **统计与导出**：生成已交/未交/待确认表格并可导出。

## 小程序端功能规划
### 0) 首页/概览
- 今日作业提醒、未确认数量、最近任务入口
- 最近一次作业统计概览

### 1) 班级与名单
- 新建班级、导入名单（Excel/CSV）
- 学生信息：姓名、学号/座号、班级
- 名单校验：空姓名、重复姓名提示

### 2) 作业任务管理
- 创建作业任务（科目、日期、备注）
- 历史任务列表
- 任务详情页：已交/未交/待确认统计

### 3) 拍照与批量识别
- 调用小程序相机拍照（支持连续拍照）
- 批量上传图片
- 图片预览与裁剪（可选）
- 上传进度与失败重试

### 4) 识别与确认
- OCR 结果展示
- 低置信度自动进入“待确认”
- 手动纠正姓名、删除误识别
- 误识别反馈：用于后续优化

### 5) 统计与导出
- 已交/未交/待确认统计
- 导出 Excel/CSV
- 支持按班级/作业任务筛选导出

## 技术方案建议
### 前端（小程序）
- 微信小程序原生 / uni-app
- 主要能力：相机/相册、图片预览、上传、列表与表单

### 后端服务
- **API 服务**：FastAPI / Node.js
- **OCR 服务**：
  - 自建（PaddleOCR / EasyOCR）
  - 或第三方 API（百度、腾讯、阿里、华为 OCR）
- **数据库**：
  - 小规模：SQLite
  - 中等规模：PostgreSQL / MySQL

### 部署形态
- 小程序前端部署在微信生态
- 后端部署在云服务器/容器（带对象存储用于图片）
- OCR 作为服务独立部署，或调用第三方 API

### 核心匹配策略
- 精确匹配：OCR 结果与名单完全一致
- 模糊匹配：
  - 编辑距离（Levenshtein）
  - 拼音相似度
  - 去除空格/符号
- 置信度规则：低于阈值进入“待确认”
- 同名处理：通过学号/座号 + 人工确认

## 数据模型（简化）
- **class**：班级
- **student**：学生（姓名、学号、班级 ID）
- **homework_task**：作业任务（科目、日期、备注）
- **upload_image**：上传图片（路径、OCR 原文、置信度）
- **submission**：提交记录（任务 ID、学生 ID、状态、识别来源）

## API 草案（简化）
- `POST /classes` 创建班级
- `POST /students/import` 导入名单（Excel/CSV）
- `POST /homework_tasks` 创建作业任务
- `POST /images/upload` 上传图片
- `POST /ocr/recognize` OCR 识别
- `POST /submissions/confirm` 人工确认结果
- `GET /reports/homework/{taskId}` 获取统计报表

## UI 页面建议
- 首页（概览 + 快捷入口）
- 班级列表 / 班级详情（学生管理）
- 作业任务列表 / 任务详情
- 拍照识别页（连续拍照 + 图片列表）
- 识别确认页（候选姓名 + 置信度）
- 统计导出页

## 关键指标与验收标准
- OCR 识别准确率（姓名区域）> 90%（良好拍摄条件）
- 拍照→统计出结果全流程 < 2 分钟（30 本以内）
- 待确认比例 < 20%（初期目标）
- 导出表格字段完整、格式可直接用于班级记录

## MVP 里程碑
1. **第 1 周**：名单管理 + 作业任务创建
2. **第 2 周**：拍照上传 + OCR 识别
3. **第 3 周**：匹配与统计导出
4. **第 4 周**：异常确认与体验优化

## 风险与应对
- **识别率不足**：增加裁剪/指引拍照角度、采用中文优化 OCR
- **同名学生**：引入学号/座号确认
- **封面样式不统一**：提供可选“姓名位置框选”功能
- **隐私合规**：图片存储与访问权限控制、数据脱敏导出

## 交付物清单
- 小程序端（拍照/上传/确认/统计）
- 后端 API（OCR/名单/作业/统计）
- 数据库结构文档
- 使用说明与维护指南
